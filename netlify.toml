// ... existing code ... <images configuration>

[build]
  command = "rm -rf .next node_modules bun.lockb package-lock.json yarn.lock .swc && bun install --no-cache --force && rm -rf .next && bun run build"
  publish = ".next"

// ... existing code ... <environment and plugins configuration>
